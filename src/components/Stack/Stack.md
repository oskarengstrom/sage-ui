# Stack

```
<Stack>
    <div>...</div>
    <div>...</div>
    <div>...</div>
</Stack>
```

## Props

`gap` unit[] | number[] (rem)

`flexDirection` string[] (default: row)

`alignItems` string[] (default: start)

`justifyContent` string[] (default: start)

`dev` bool

`as` element

{sizeMixins}

{spaceMixins}
